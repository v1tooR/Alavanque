Seletor nativo estilizado. Aceita `options` como array de strings ou `{value,label}`.

```jsx
<Select label="Método" placeholder="Escolha" options={[{value:'esteira',label:'Esteira manual'},{value:'bandeja',label:'Bandeja'}]} />
```
